# Screenshots

Three images, referenced from the project README. Replace these placeholders
with the real captures at the same filenames.

| File | What it should show |
|---|---|
| `landing.png` | The landing page, top of the fold |
| `columns.png` | The column check screen, everything auto-detected |
| `story.png` | Board view on a decomposition finding, showing the contribution chart |

Captured at desktop width. Keep them under about 400KB each so the README
stays quick to load.
